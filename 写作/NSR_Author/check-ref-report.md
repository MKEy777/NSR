# 参考文献核查报告

生成时间：2026-08-03  
核查范围：`main.tex`、`supplement.tex` 与 `nsr_sample.bib`。

本报告为只读审计。核查了 UTF-8 可读性、BibTeX 条目解析、正文引用键、已生成的 BibTeX 日志，以及本目录已有的逐条元数据审计记录 `reference-check-report.md`（Crossref/DOI、OpenReview、DBLP 和正式会议论文集）。本次补充的 OpenAlex/Semantic Scholar 串行复核因当前网络请求连续超时而未完成，故不以其未返回候选来否定已由 DOI/正式来源确认的条目。未修改任何 `.tex` 或 `.bib` 文件。

## 总体摘要

| 维度 | 总数 | 通过 | 不通过/有疑问 |
|---|---:|---:|---:|
| 文献真实性（全部 BibTeX 条目） | 65 | 64 | 1 |
| 正文引用内容匹配（去重引用 key） | 54 | 53 | 1 |
| 引用键可解析性 | 54 | 54 | 0 |

主文献表实际输出 46 条文献；补充材料有 12 个引用命令。`main.blg` 中没有未定义引用或 BibTeX 栈错误。唯一 BibTeX 警告为 `ref25` 缺少页码；ICLR 2016 的该论文没有连续的正式论文集页码，不能补造页码，该警告不影响条目真实性或正文引用。

## 维度一：参考文献真实性

65 条条目均可正常解析，标题、首/末作者和出版年份均已在现有逐条审计中与权威记录比对。64 条通过。具体的逐条核验路由和字段比对见同目录的 `reference-check-report.md`。

### 有疑问

| Key | 条目 | 疑点类型 | 说明 |
|---|---|---|---|
| ref57 | SqueezeNet: AlexNet-Level Accuracy with 50x Fewer Parameters | 出版政策风险 | 条目本身真实且作者、标题、年份一致，但它是 arXiv 预印本，未发现可替代的同行评审正式版本。若 NSR 严格执行“已发表或 in-press”偏好，应由作者决定是否保留。 |

### 字段与编码复核

- `ref3` 的 `Björn W. Schuller` 与 `ref46` 的 `Woźniak` 均为有效 UTF-8，非乱码。
- `ref46` 的文章号为 `6793`；DOI 中的 `51110` 不是文章号。
- `ref25` 未设 `pages` 是来源出版形式决定的正常情况。
- 其余 64 条不存在关键字段（标题、作者、年份）不一致的问题。

## 维度二：正文引用内容匹配

全部现有引用点的语境已核对。53 个去重引用 key 与其所支撑的主题、方法或数据集相符，包括：EEG 数据集与 PSE 预处理、TTFS/SNN 动力学、知识蒸馏与量化、LSTM 硬件依赖、低功耗时钟切换、AER 和四相 bundled-data 握手。

### 有疑问

| 位置 | 引用 key | 疑点类型 | 说明 |
|---|---|---|---|
| `main.tex` Table 1 | ref52 | 相关性有限 | 该文是随机森林的通用综述，而非 EEG 情绪识别的原始随机森林基线论文。它足以标识所用分类器，不能单独支撑 EEG 特异性的性能或方法结论。当前表格仅用它标识基线，未构成错误引用。 |

## 其他发现

- 没有未定义 key；未引用的 BibTeX 条目是 `ref2`、`ref6`、`ref7`、`ref8`、`ref29`、`ref30`、`ref43`、`ref66`、`ref67`、`ref68`、`ref70`。这不是编译错误，只是条目冗余。
- 之前的“神经编码、LSTM 硬件、知识蒸馏、四相握手、LIF 动力学”引用错配已通过 `ref72`--`ref79` 的替换或收窄措辞解决。
- 所检查文件均可按 UTF-8 严格读取，未发现替换字符、无效字节或异常 CJK 字符。

## 结论

除 `ref57` 的预印本政策风险及 `ref52` 的通用性限制外，当前参考文献的真实性和正文引用匹配均可通过。报告未对论文原文或 `.bib` 文件作任何修改。
